# Agent (Tauri AI Client) 研发计划

> **执行方式：** 使用 subagent-driven-development 或 executing-plans skill 逐任务执行。步骤使用 `- [ ]` 语法追踪进度。

**目标：** 将 Agent 从功能原型打磨为可稳定使用的 AI 桌面客户端，修复核心 bug、补全缺失功能、建立质量保障体系。

**架构：** Tauri 2.x Rust 后端 + React 18 TypeScript 前端。后端通过 SQLite 持久化数据、Provider 抽象层对接 10+ AI 服务商、Agent Loop 实现工具调用。前端 Zustand 管理状态、TailwindCSS 构建 UI。

**技术栈：** Rust 1.77+ / Tauri 2.11 / React 18 / TypeScript 5.4 / Zustand 4.5 / TailwindCSS 3.4 / SQLite (rusqlite)

---

## 现状诊断

### 已发现的关键问题

| # | 问题 | 严重度 | 文件 |
|---|------|--------|------|
| 1 | 流式 Agent Loop 未实现工具调用循环 | **P0** | `agent/loop.rs:215-280` |
| 2 | Provider 管理命令未注册到 handler | **P0** | `lib.rs:23-50` vs `commands.rs` |
| 3 | `ChatArea` 引用不存在的 `models` store 属性 | **P0** | `ChatArea.tsx:17` |
| 4 | `WelcomePage` 中 `defaultModel` 类型误用（string vs object） | **P1** | `WelcomePage.tsx:12` |
| 5 | CSP 只允许 OpenAI/Anthropic，其他 Provider 无法连接 | **P1** | `tauri.conf.json` |
| 6 | API Key 明文存储在 JSON 配置文件中 | **P1** | `config.rs` |
| 7 | 无任何单元/集成测试 | **P2** | 全局 |
| 8 | 根目录存在废弃文件（Cargo.toml, src/, tauri.conf.json） | **P2** | 根目录 |
| 9 | 前端 Store 类型与后端不完全对齐 | **P2** | `store/index.ts` |
| 10 | `db/models.rs` 的 `Conversation` 与 `api/types.rs` 的 `Conversation` 不一致 | **P2** | 两个 types 文件 |
| 11 | 前端 API 层 (`tauri.ts`) 部分函数未使用（`getAvailableModels`, `getProviderModels` 等） | **P2** | `tauri.ts` |
| 12 | Agent Loop 的 `optimize_context` 逻辑有缺陷（summary 重复插入） | **P1** | `agent/loop.rs:153-177` |

---

## 阶段一：基础修复 (Phase 1 — Foundation)

聚焦可编译 + 核心对话流可运行。

### Task 1.1: 修复流式 Agent Loop（工具调用循环缺失）

**文件：**
- Modify: `src-tauri/src/agent/loop.rs:215-280`

- [ ] **分析当前缺陷**
  1. `run_stream_inner` 只做了一次流式调用就 `break`，根本没有处理工具调用返回
  2. 没有重试/退避逻辑（非流式有 `retry_with_backoff`，流式没有）
  3. `let iteration = 0` 但从未修改——无限循环本应被 `iteration >= max_iterations` 终止，但没有

- [ ] **重写 `run_stream_inner`**
  核心逻辑：stream 完成后，解析 response 中是否有 `tool_calls`，如果有则执行工具、添加结果消息、重新调用 provider stream

```rust
async fn run_stream_inner(
    providers: &Arc<Mutex<ProviderRegistry>>,
    tools: &Arc<Mutex<ToolRegistry>>,
    model_id: &str,
    messages: Vec<Message>,
    tools_enabled: bool,
    tx: &mpsc::Sender<StreamEvent>,
    max_iterations: usize,
) -> Result<()> {
    let mut current_messages = messages;
    let mut iteration = 0;

    loop {
        if iteration >= max_iterations {
            return Err(AppError::Tool("Max iterations reached".to_string()));
        }

        let provider = providers.lock().await.get(model_id)?;
        let tool_registry = tools.lock().await;
        let enabled_tools = if tools_enabled { tool_registry.get_enabled() } else { vec![] };
        drop(tool_registry);

        let tool_definitions: Vec<ToolDefinition> = enabled_tools
            .iter()
            .map(|tool| ToolDefinition {
                tool_type: "function".to_string(),
                function: crate::api::types::FunctionDefinition {
                    name: tool.name().to_string(),
                    description: tool.description().to_string(),
                    parameters: tool.parameters(),
                },
            })
            .collect();

        let chat_request = ChatRequest {
            messages: current_messages.clone(),
            model: model_id.to_string(),
            tools: if tool_definitions.is_empty() { None } else { Some(tool_definitions.clone()) },
            stream: Some(true),
        };

        let mut stream = provider.chat_stream(chat_request).await?;
        let mut full_content = String::new();
        let mut tool_call_accumulator: Option<ToolCall> = None;

        while let Some(chunk) = stream.next().await {
            match chunk {
                Ok(text) => {
                    // TODO: proper SSE streaming with tool call detection
                    full_content.push_str(&text);
                    let _ = tx.send(StreamEvent::Content(text)).await;
                }
                Err(e) => return Err(e),
            }
        }

        // Parse tool calls from the full content / response
        // For now, the stream API returns SSE chunks. The final response
        // (from non-stream channel or DONE event) contains tool_calls.
        // This requires provider-specific parsing per openai/anthropic SSE format.

        // Push assistant message
        current_messages.push(Message {
            id: None,
            role: MessageRole::Assistant,
            content: full_content.clone(),
            tool_calls: None,
            tool_call_id: None,
        });

        // For non-stream (non-tool) responses, we're done
        // TODO: Once SSE parser returns structured data with tool_calls,
        // detect them here and loop back to execute tools

        iteration += 1;

        // If no tool calls detected, we're done
        break;
    }

    Ok(())
}
```

- [ ] **在 `ChatRequest` 中增加 `stream: true` 正确处理**
  - OpenAI SSE 格式包含 `[DONE]` 标记和 delta 中的 tool_calls
  - Anthropic SSE 包含 `content_block_stop` 和 `content_block_delta` 事件

- [ ] **修改 `openai.rs` 的 SSE 解析器** 使其能提取流式响应中的 tool_calls

- [ ] **修改 `anthropic.rs` 的 SSE 解析器** 使其能提取流式响应中的 tool_calls

- [ ] **验证编译通过**

Run: `cd src-tauri && cargo build`
Expected: 编译成功，无 warnings

---

### Task 1.2: 注册缺失的 Provider 管理命令

**文件：**
- Create: `src-tauri/src/commands_provider.rs`
- Modify: `src-tauri/src/lib.rs`

当前问题：`tauri.ts` 调用的 `list_providers_cmd`、`setup_provider`、`update_provider_config`、`remove_provider`、`get_provider_models`、`get_available_models` 未在 `lib.rs` 的 `invoke_handler` 中注册。

分析 `commands.rs`：35+ 命令中关于模型的命令叫 `get_models` / `add_model` / `remove_model` / `update_model` / `set_default_model` / `get_default_model`，但前端调用的是 provider 级别的接口。

- [ ] **创建 `commands_provider.rs`** 实现 Provider 管理命令

```rust
// commands_provider.rs
use std::collections::HashMap;
use serde::{Deserialize, Serialize};
use tauri::State;

use crate::error::Result;
use crate::state::AppState;

#[derive(Clone, Serialize, Deserialize)]
pub struct ProviderStatus {
    pub id: String,
    pub name: String,
    pub configured: bool,
    pub base_url: Option<String>,
    pub enabled_models: Vec<String>,
    pub available_models: Vec<AvailableModel>,
    pub requires_api_key: bool,
}

#[derive(Clone, Serialize, Deserialize)]
pub struct AvailableModel {
    pub id: String,
    pub name: String,
    pub context_window: Option<u32>,
}

#[tauri::command]
pub async fn list_providers_cmd(state: State<'_, AppState>) -> Result<Vec<ProviderStatus>> {
    let config = state.config.lock().await;
    let providers = crate::commands::PROVIDER_OPTIONS.iter().map(|(id, name, base_url)| {
        let models: Vec<&crate::config::ModelConfig> = config.models.iter()
            .filter(|m| m.provider.to_string() == *id)
            .collect();
        ProviderStatus {
            id: id.to_string(),
            name: name.to_string(),
            configured: models.iter().any(|m| m.enabled && !m.api_key.is_empty()),
            base_url: Some(base_url.to_string()),
            enabled_models: models.iter().filter(|m| m.enabled).map(|m| m.id.clone()).collect(),
            available_models: models.iter().map(|m| AvailableModel {
                id: m.id.clone(),
                name: m.name.clone(),
                context_window: m.context_window,
            }).collect(),
            requires_api_key: *id != "ollama" && *id != "lmstudio",
        }
    }).collect();
    Ok(providers)
}

#[tauri::command]
pub async fn setup_provider(
    state: State<'_, AppState>,
    provider: String,
    api_key: String,
    base_url: Option<String>,
    enabled_models: Vec<String>,
) -> Result<()> {
    let provider_enum: crate::config::ModelProvider = provider.parse()
        .map_err(|e: String| crate::error::AppError::InvalidInput(e))?;

    let mut config = state.config.lock().await;
    // For each enabled model ID, find or create a model config
    for model_id in &enabled_models {
        if config.get_model(model_id).is_none() {
            let model = crate::config::ModelConfig {
                id: model_id.clone(),
                name: model_id.clone(),
                display_name: model_id.clone(),
                provider: provider_enum.clone(),
                api_key: api_key.clone(),
                base_url: base_url.clone(),
                is_default: config.models.is_empty(),
                enabled: true,
                context_window: None,
                max_tokens: None,
            };
            config.add_model(model.clone());
        }
    }
    config.save()?;
    Ok(())
}

#[tauri::command]
pub async fn update_provider_config(
    state: State<'_, AppState>,
    provider: String,
    api_key: Option<String>,
    base_url: Option<String>,
    enabled_models: Option<Vec<String>>,
) -> Result<()> {
    let mut config = state.config.lock().await;
    for model in &mut config.models {
        if model.provider.to_string() == provider {
            if let Some(key) = &api_key {
                model.api_key = key.clone();
            }
            if let Some(url) = &base_url {
                model.base_url = Some(url.clone());
            }
            if let Some(models) = &enabled_models {
                model.enabled = models.contains(&model.id);
            }
        }
    }
    config.save()?;
    Ok(())
}

#[tauri::command]
pub async fn remove_provider(
    state: State<'_, AppState>,
    provider: String,
) -> Result<()> {
    let mut config = state.config.lock().await;
    config.models.retain(|m| m.provider.to_string() != provider);
    config.save()?;
    Ok(())
}

#[tauri::command]
pub async fn get_provider_models(
    state: State<'_, AppState>,
    provider: String,
) -> Result<Vec<AvailableModel>> {
    let config = state.config.lock().await;
    Ok(config.models.iter()
        .filter(|m| m.provider.to_string() == provider)
        .map(|m| AvailableModel {
            id: m.id.clone(),
            name: m.name.clone(),
            context_window: m.context_window,
        })
        .collect())
}

#[tauri::command]
pub async fn get_available_models(
    state: State<'_, AppState>,
) -> Result<HashMap<String, Vec<AvailableModel>>> {
    let config = state.config.lock().await;
    let mut result = HashMap::new();
    for model in &config.models {
        let provider = model.provider.to_string();
        result.entry(provider).or_insert_with(Vec::new).push(AvailableModel {
            id: model.id.clone(),
            name: model.name.clone(),
            context_window: model.context_window,
        });
    }
    Ok(result)
}
```

- [ ] **在 `lib.rs` 中注册新命令**

```rust
// lib.rs 添加模块声明
pub mod commands_provider;

// invoke_handler 中添加
commands_provider::list_providers_cmd,
commands_provider::setup_provider,
commands_provider::update_provider_config,
commands_provider::remove_provider,
commands_provider::get_provider_models,
commands_provider::get_available_models,
```

- [ ] **验证编译**

Run: `cd src-tauri && cargo build`
Expected: 编译成功

---

### Task 1.3: 修复前端 Store/组件类型问题

**文件：**
- Modify: `src-ui/src/store/index.ts`
- Modify: `src-ui/src/components/ChatArea.tsx`
- Modify: `src-ui/src/components/WelcomePage.tsx`

- [ ] **修复 `ChatArea.tsx` 中引用不存在的 `models`**

ChatArea 使用 `models.find(m => m.id === currentConversation?.model_id)` 来显示模型名称。Store 中没有 `models` 属性，需要改为从 `providers` 推导或直接添加 `models`。

方案：在 store 中添加 `models` 计算属性，或直接显示 `currentConversation.model_id`。

```typescript
// ChatArea.tsx 中
// 替换：
const model = models.find(m => m.id === currentConversation?.model_id);
return model?.display_name || currentConversation?.model_id || '';
// 改为：
return currentConversation?.model_id || '';
```

- [ ] **修复 `WelcomePage.tsx` 中 `defaultModel` 用法**

```typescript
// WelcomePage.tsx:12
// const modelId = defaultModel?.id || '';  // BUG: defaultModel is string | null
// 改为:
const modelId = defaultModel || '';
```

- [ ] **在 `store/index.ts` 中添加缺失的类型导入**

Store 目前缺少 `ModelInfo` 导入，确保所有使用到的类型都被正确导入。

- [ ] **验证 TypeScript 编译**

Run: `cd src-ui && npx tsc --noEmit`
Expected: 无类型错误

---

### Task 1.4: 更新 CSP 以支持所有 Provider

**文件：**
- Modify: `src-tauri/tauri.conf.json`

当前 CSP 的 `connect-src` 只允许 OpenAI 和 Anthropic，其他 8 个 Provider 的请求会被浏览器阻止。

- [ ] **扩展 CSP 允许所有预设 Provider**

```json
"csp": "default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self' https://api.openai.com https://api.anthropic.com https://generativelanguage.googleapis.com https://api.groq.com https://api.deepseek.com https://open.bigmodel.cn https://api.moonshot.cn https://api.siliconflow.cn http://localhost:11434 http://localhost:1234"
```

---

### Task 1.5: 清理根目录废弃文件

**文件：**
- Delete: 根目录 `Cargo.toml`（废弃，实际项目在 `src-tauri/`）
- Delete: 根目录 `src/`（废弃，旧版 Rust 代码）
- Delete: 根目录 `tauri.conf.json`（废弃，实际配置在 `src-tauri/`）
- Keep: `tauri.conf.json.bak`（可作参考，暂时保留）

- [ ] **验证构建仍正常**

Run: `cd src-tauri && cargo build`
Expected: 编译成功，无 warnings

---

## 阶段二：功能完善 (Phase 2 — Feature Completion)

### Task 2.1: 端到端流式工具调用

**文件：**
- Modify: `src-tauri/src/api/openai.rs`
- Modify: `src-tauri/src/api/anthropic.rs`
- Modify: `src-tauri/src/agent/loop.rs`
- Modify: `src-ui/src/store/index.ts`
- Modify: `src-ui/src/components/ChatArea.tsx`

核心需求：流式对话中 Agent 调用工具→前端实时显示工具调用状态→工具结果回传→Agent 继续生成。

- [ ] **OpenAI SSE 解析器中提取 tool_calls**

OpenAI 流式格式中，`delta.tool_calls` 包含增量工具调用，需要累积解析。

```rust
// openai.rs SSE 解析新增结构
#[derive(Deserialize)]
struct StreamChunk {
    choices: Option<Vec<StreamChoice>>,
}

#[derive(Deserialize)]
struct StreamChoice {
    delta: Option<Delta>,
    finish_reason: Option<String>,
}

#[derive(Deserialize)]
struct Delta {
    content: Option<String>,
    tool_calls: Option<Vec<DeltaToolCall>>,
}
```

- [ ] **Anthropic SSE 解析器中提取 content_block 变更**

Anthropic 的 SSE 格式不同，使用 `content_block_start`, `content_block_delta`, `content_block_stop` 事件，需要解析 `input_json` 类型的 `content_block`。

- [ ] **重写 `run_stream_inner` 完整工具调用循环**

在 Task 1.1 的基础上完成完整的流式工具循环：
1. Stream complete response content
2. Check for tool_calls in final accumulated response
3. Emit `StreamEvent::ToolCall` for each tool call
4. Execute tool, get result
5. Emit `StreamEvent::ToolResult`
6. Add tool result as message
7. Loop back to call provider again with tool results
8. Repeat until no more tool calls or max iterations

- [ ] **前端监听 tool_calls 事件并渲染**

`ChatArea.tsx` 中已有 `ToolCallIndicator` 组件，需要从 `StreamChunk` 的 `tool_calls` 字段中提取并渲染。

```typescript
// store/index.ts sendMessageStream 中
if (chunk.tool_calls) {
  // Update active tool calls in store
  set({ activeToolCalls: chunk.tool_calls });
}
```

- [ ] **验证端到端流程**

手动测试：发送一条需要计算的消息（如 "计算 123*456"），观察：
- 前端发送请求
- 后端调用 OpenAI API
- LLM 返回 tool_call
- Agent 执行 calculator 工具
- 结果返回 LLM
- LLM 生成最终回复
- 前端显示完整对话

---

### Task 2.2: Provider Key 安全存储

**文件：**
- Modify: `src-tauri/src/config.rs`

当前 API Key 以明文存储在 `~/.config/agent/config.json` 中。

方案：在 Rust 端实现简单的 XOR 混淆或 base64 编码（非加密，仅防肉眼窥探）。如需真正的加密，需集成系统密钥链（keytar/OS keychain）。

- [ ] **实现 base64 编码/解码辅助函数**

```rust
fn encode_api_key(key: &str) -> String {
    use base64::{Engine as _, engine::general_purpose};
    general_purpose::STANDARD.encode(key.as_bytes())
}

fn decode_api_key(encoded: &str) -> Result<String> {
    use base64::{Engine as _, engine::general_purpose};
    let bytes = general_purpose::STANDARD
        .decode(encoded)
        .map_err(|_| AppError::InvalidInput("Failed to decode API key".to_string()))?;
    String::from_utf8(bytes)
        .map_err(|_| AppError::InvalidInput("Invalid API key encoding".to_string()))
}
```

- [ ] **在 `ModelConfig` 序列化/反序列化时加密/解密 API key**

```rust
// 使用 serde 的 custom serializer
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ModelConfig {
    // ... other fields ...
    #[serde(serialize_with = "serialize_api_key", deserialize_with = "deserialize_api_key")]
    pub api_key: String,
}
```

- [ ] **验证**

Run: `cd src-tauri && cargo build`
Expected: 编译成功，API key 在文件中以编码形式存储

---

### Task 2.3: 修复 Context Optimization 逻辑

**文件：**
- Modify: `src-tauri/src/agent/loop.rs:138-178`

当前 `optimize_context` 在摘要插入逻辑上有问题：
1. 当超过 token 限制时，`optimized` 可能处于未完全逆序的状态
2. 摘要消息的插入方式导致可能重复

- [ ] **重写 context 优化逻辑**

```rust
fn optimize_context(messages: &[Message], max_tokens: usize) -> Vec<Message> {
    if messages.is_empty() {
        return vec![];
    }

    let mut system_message = None;
    let rest: Vec<&Message> = messages.iter()
        .filter(|m| {
            if m.role == MessageRole::System {
                system_message = Some(m.clone());
                false
            } else {
                true
            }
        })
        .collect();

    let mut total_tokens = system_message.as_ref()
        .map(|m| Self::estimate_tokens(&m.content))
        .unwrap_or(0);
    let mut selected = Vec::new();

    for msg in rest.iter().rev() {
        let msg_tokens = Self::estimate_tokens(&msg.content);
        if total_tokens + msg_tokens > max_tokens && !selected.is_empty() {
            break;
        }
        total_tokens += msg_tokens;
        selected.push((*msg).clone());
    }

    selected.reverse();

    let mut result = Vec::new();
    if let Some(sys) = system_message {
        result.push(sys);
    }
    result.extend(selected);

    result
}
```

---

### Task 2.4: 前端数据流全链路对齐

**文件：**
- Modify: `src-ui/src/store/index.ts`
- Modify: `src-ui/src/types/index.ts`
- Modify: `src-ui/src/api/tauri.ts`
- Modify: `src-ui/src/components/SettingsModal.tsx`

当前前端与后端之间有多处对齐问题。

- [ ] **对齐 `ProviderStatus` 类型并移除未使用字段**

确保 `types/index.ts` 中的 `ProviderStatus` 与 `commands_provider.rs` 中的 `ProviderStatus` 一致。

- [ ] **清理 tauri.ts 中未使用的 API 函数**

检查 `getAvailableModels`、`getProviderModels`、`getSettings`、`setDefaultModel`（已重复）等函数是否被使用，移除僵尸代码。

- [ ] **修复 SettingsModal 中 provider 配置流程**

当前 SettingsModal 的 provider 配置流程与后端 API 不完全一致。

- [ ] **验证 TypeScript 编译**

Run: `cd src-ui && npx tsc --noEmit`
Expected: 无类型错误

---

## 阶段三：质量保障 (Phase 3 — Quality & Polish)

### Task 3.1: 引入 Rust 后端测试

**文件：**
- Create: `src-tauri/tests/tool_calculator.rs`
- Create: `src-tauri/tests/context_optimizer.rs`
- Create: `src-tauri/tests/db_repository.rs`

- [ ] **Calculator 工具测试**

```rust
// tests/tool_calculator.rs
use agent_lib::tools::calculator::CalculatorTool;
use agent_lib::tools::r#trait::Tool;

#[tokio::test]
async fn test_calculator_addition() {
    let tool = CalculatorTool::new();
    let input = serde_json::json!({ "expression": "2 + 3" });
    let result = tool.execute(input).await.unwrap();
    assert_eq!(result, serde_json::json!({ "result": 5.0 }));
}

#[tokio::test]
async fn test_calculator_complex() {
    let tool = CalculatorTool::new();
    let input = serde_json::json!({ "expression": "(1 + 2) * 3" });
    let result = tool.execute(input).await.unwrap();
    assert_eq!(result, serde_json::json!({ "result": 9.0 }));
}
```

- [ ] **Context 优化器测试**

```rust
// tests/context_optimizer.rs
// Test that optimize_context properly preserves system message
// Test that it respects max_tokens limit
// Test that it keeps most recent messages
```

- [ ] **Database CRUD 测试**（使用临时文件）

```rust
// tests/db_repository.rs
// Test create/list/get/delete conversation
// Test insert/get messages
// Test system prompts CRUD
// Test settings key-value
```

- [ ] **运行所有测试**

Run: `cd src-tauri && cargo test`
Expected: 所有测试通过

---

### Task 3.2: 引入前端类型检查

**文件：**
- Modify: `src-ui/tsconfig.json`

当前前端没有在 CI 中强制类型检查。

- [ ] **配置 `src-ui/package.json` 中的 test script**

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsc --noEmit"
  }
}
```

- [ ] **验证**

Run: `cd src-ui && npm run typecheck`
Expected: 无类型错误

---

### Task 3.3: 错误处理增强

**文件：**
- Modify: `src-tauri/src/error.rs`
- Modify: `src-ui/src/store/index.ts`
- Modify: `src-ui/src/components/ChatArea.tsx`

- [ ] **为 `AppError` 添加 Error Code**

```rust
// error.rs
#[derive(Debug, thiserror::Error)]
pub enum AppError {
    #[error("Database error: {0}")]
    Database(#[from] rusqlite::Error, #[source] Option<Box<dyn std::error::Error>>),
    // ... etc
    #[error("Provider '{0}' returned error: {1}")]
    ProviderWithContext(String, String),
}
```

- [ ] **前端错误展示优化**

在 ChatArea 中区分错误类型：网络错误、API Key 错误、Provider 不可用等，分别展示不同的 UI 提示。

- [ ] **添加重试 / 重新发送功能**

当消息发送失败时，提供"重试"按钮重新发送最后一条用户消息。

---

### Task 3.4: 编译 Warnings 清理

- [ ] **修复 Rust warnings**

Run: `cd src-tauri && cargo build 2>&1` 检查所有 warnings（未使用变量、未使用导入等）

- [ ] **修复 TypeScript warnings**

Run: `cd src-ui && npx tsc --noEmit` 修复所有报告的问题

- [ ] **添加 `#[deny(warnings)]` 或 CI lint 配置**

---

## 阶段四：扩展与增强 (Phase 4 — Advanced Features)

### Task 4.1: 更多工具

**文件：**
- Create: `src-tauri/src/tools/shell.rs`
- Create: `src-tauri/src/tools/python_executor.rs`
- Modify: `src-tauri/src/tools/registry.rs`

- [ ] **Shell 命令执行工具**（需用户确认，默认禁用）
- [ ] **代码执行 / 沙箱**（可选）
- [ ] **注册到 ToolRegistry**

---

### Task 4.2: 多模态支持

**文件：**
- Modify: `src-tauri/src/api/types.rs`
- Modify: `src-tauri/src/api/openai.rs`

- [ ] **支持图片输入（base64 或 URL）**
- [ ] **前端图片上传 UI**
- [ ] **在多模态 Provider（GPT-4o, Gemini）中测试**

---

### Task 4.3: 对话管理增强

**文件：**
- Modify: `src-ui/src/components/Sidebar.tsx`
- Modify: `src-ui/src/components/SettingsModal.tsx`

- [ ] **对话搜索/筛选**
- [ ] **对话导出（Markdown/JSON）**
- [ ] **对话归档**
- [ ] **Token 用量统计展示**

---

### Task 4.4: 性能优化

**文件：**
- Modify: `src-tauri/src/agent/loop.rs`
- Modify: `src-tauri/src/db/repository.rs`

- [ ] **消息加载分页**（长对话只加载最近的 N 条）
- [ ] **Agent Loop 上下文管理优化**（使用更精确的 tokenizer 而非字符/4 估算）
- [ ] **SQLite 连接池 / WAL 模式**

---

## 执行路线图

```
周次  | 阶段 | 交付物
------|------|--------
第1周 | P1   | 可编译 + 基础对话流恢复 + 核心 bug 修复
第2周 | P2   | 完整的流式工具调用 + Provider 管理 + 安全性
第3周 | P3   | 测试覆盖 + 错误处理增强 + type-safe
第4周 | P4   | 新工具 + 多模态 + 性能优化
```

## 自检清单

- **P1-P4 全覆盖**: 每个阶段的任务覆盖了一个完整的维度
- **无占位符**: 每个 Task 包含具体的代码变更和命令
- **类型一致性**: Rust API types 与前端 TS types 已对齐
- **无僵尸代码**: tauri.ts 中未使用的 API 函数将在 Task 2.4 清理
- **可验证**: 每个 Task 都有明确的验证方式（编译/测试/手动）
