# Disallow unwrap/expect in production code
disallowed-methods = [
  { path = "std::option::Option::unwrap", reason = "use ? or proper error handling" },
  { path = "std::result::Result::unwrap", reason = "use ? or proper error handling" },
  { path = "std::option::Option::expect", reason = "use ? or proper error handling" },
  { path = "std::result::Result::expect", reason = "use ? or proper error handling" },
]
